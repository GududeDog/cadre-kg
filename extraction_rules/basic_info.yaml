category: basic_info
display_name: 基础信息

resume_rules:
  - '从简历文本中提取每一行任职经历，包括学生时期'
  - '只提取职务名称原文到 position 字段，rank 字段统一填空字符串""，职务级别和职级级别将由后续规则引擎推断，不需要在本次提取中推断'
  - '每一行任职经历生成一个独立的Resume实体，不合并、不遗漏、不编造'
